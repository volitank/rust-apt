[package]
authors = [
    "Blake Lee (Volitank) <blake@volian.org>",
    "Chris West (Faux) <git@goeswhere.com>"
]
categories = [
    "api-bindings",
    "os",
]
description = "Bindings for libapt-pkg"
license = "GPL-3.0-or-later"
name = "rust-apt"
readme = "README.md"
repository = "https://gitlab.com/volian/rust-apt"
version = "0.1.0"

edition = "2021"

[dependencies]
cxx = "1.0"

[build-dependencies]
cxx-build = "1.0"
